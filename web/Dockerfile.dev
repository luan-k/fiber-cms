FROM node:22-alpine
WORKDIR /app

# Copy package files
COPY package*.json ./
RUN npm install

# Don't copy source - will be mounted as volume
EXPOSE 4321

# Start Astro dev server with polling for Docker
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "4321"]